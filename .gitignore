.zig-cache/
zig-out/
zig-pkg/
*.o
*.a
*.dylib
*.so

# Test artifacts
tests/fixtures/output/

# Stray catalog DBs from runs where XDG_DATA_HOME wasn't isolated.
/mediastacks/

# Playwright MCP snapshots from local UI-test sessions.
.playwright-mcp/

# Local Playwright install (test-only, not a runtime dep).
/node_modules/
/package.json
/package-lock.json

# OS
.DS_Store
.completions/

# Design/process docs (specs, plans, brainstorming) — local only, never committed.
docs/superpowers/

# nix build symlink
/result

# local dev (playwright for screenshots)
node_modules/
package-lock.json
